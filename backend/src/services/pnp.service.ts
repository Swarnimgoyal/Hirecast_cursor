// This service layer is intentionally thin for now.
// It exists to demonstrate where additional business logic would live
// if/when the PNP adapter starts talking to a real blockchain SDK.

export {};

